return {
	{
		'github/copilot.vim',
		lazy = false,
		name = 'github-copilot',
		config = function()
			-- require('github-copilot').setup({
			-- })

			vim.keymap.set('i', '<C-TAB>', 'copilot#Accept("<CR>")', { silent = true, expr = true })
		end
	}
}
